pipeline {
  agent any

  environment {
    IMAGE_NAME = 'devops_nodejs:latest'
    DOCKERHUB_CREDENTIALS = 'jenkins-dockerhub'  
    DOCKERHUB_REPO = 'ramyasree15/devops_nodejs' 
  }

  stages {
    stage('Checkout Code') {
      steps {
        git branch: 'main', url: 'https://github.com/ramyasree1505/emc-nodejs-app.git'
      }
    }

    stage('Build Docker Image') {
      steps {
        bat "docker build -t %IMAGE_NAME% ."
      }
    }

    stage('Docker Login and Push') {
      steps {
        withCredentials([usernamePassword(credentialsId: DOCKERHUB_CREDENTIALS, usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
          bat """
            docker login -u %DOCKER_USER% -p %DOCKER_PASS%
            docker tag %IMAGE_NAME% %DOCKERHUB_REPO%:latest1
            docker push %DOCKERHUB_REPO%:latest1
          """
        }
      }
    }
    stage('Pull Docker Image') {
      steps {
        bat "docker pull %DOCKERHUB_REPO%:latest"
      }
    }

    stage('Deploy Docker Container') {
      steps {
        bat 'docker run -d -p 80:3000 --name emc-node-container %DOCKERHUB_REPO%:latest'
      }
    }
  }
}
